- title: Basic sensor with observed property
  snippets:
    - language: json
      ref: examples/sensor.json

- title: Compound sensor using the System polymorphism option
  snippets:
    - language: json
      ref: examples/compound-sensor.json

- title: Multi-parameter sensor with inline observed properties and subsystems
  snippets:
    - language: json
      ref: examples/sensor-complex.json
